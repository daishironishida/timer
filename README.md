# Timer

A minimal countdown timer for the browser.

https://daishironishida.github.io/timer/

## Features

- Start, Pause, and Stop controls
- Auto-restart mode — automatically repeats the timer when it finishes

## Usage

Open `index.html` in any browser. No build step, no dependencies.

To set a custom duration, click **⚙ Settings** below the buttons.
